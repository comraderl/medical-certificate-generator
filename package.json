{
  "name": "certificate-generator",
  "version": "1.0.0",
  "description": "Medical Certificate Generator",
  "scripts": {
    "start": "echo 'Run from client for frontend dev, or use Netlify CLI for full dev'",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.4.1",
    "helmet": "^8.1.0",
    "nodemailer": "^8.0.7",
    "serverless-http": "^3.2.0"
  }
}
